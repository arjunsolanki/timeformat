require 'spec_helper'

describe Timeformat do
  it 'has a version number' do
    expect(Timeformat::VERSION).not_to be nil
  end

  it 'does something useful' do
    expect(false).to eq(true)
  end
end
